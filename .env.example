# ============================================
# kodbank1 Environment Configuration
# ============================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# ============================================
# Database Configuration
# ============================================
# AIVEN MySQL database connection URL
# Format: mysql://username:password@host:port/database?ssl-mode=REQUIRED
# SSL mode is REQUIRED for AIVEN MySQL connections
DATABASE_URL=mysql://avnadmin:YOUR_PASSWORD_HERE@mysql-kodbank-project.aivencloud.com:12345/defaultdb?ssl-mode=REQUIRED

# ============================================
# JWT Configuration
# ============================================
# Secret key for signing JWT tokens
# IMPORTANT: Generate a strong, random secret key for production
# You can generate one using: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-secret-key-here-change-this-in-production

# JWT token expiration time in hours
# Default: 1 hour (recommended for security)
JWT_EXPIRY_HOURS=1

# ============================================
# Application Configuration
# ============================================
# Flask environment: development or production
# Use 'production' for deployed environments
FLASK_ENV=development

# Flask debug mode: True or False
# IMPORTANT: Set to False in production
FLASK_DEBUG=True

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update DATABASE_URL with your AIVEN MySQL credentials:
#    - Replace YOUR_PASSWORD_HERE with your actual database password
#    - Update host, port, and database name if different
#    - Keep ssl-mode=REQUIRED for AIVEN connections
#
# 3. Generate a secure JWT secret key:
#    python -c "import secrets; print(secrets.token_hex(32))"
#    Replace JWT_SECRET_KEY with the generated value
#
# 4. For production deployment:
#    - Set FLASK_ENV=production
#    - Set FLASK_DEBUG=False
#    - Use a strong, unique JWT_SECRET_KEY
#    - Ensure DATABASE_URL uses SSL
#
# 5. Verify your .env file is listed in .gitignore
#    to prevent committing sensitive credentials
#
# ============================================
# Security Notes
# ============================================
# - Never commit .env to version control
# - Use different credentials for development and production
# - Rotate JWT_SECRET_KEY periodically in production
# - Keep DATABASE_URL credentials secure
# - Use HTTPS in production for secure cookie transmission
